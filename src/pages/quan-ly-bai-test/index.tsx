import { NextPage } from 'next';

export interface IAssessmentProps {
  data?: 'TODO:Change me';
}

export const Assessment: NextPage = (props: IAssessmentProps) => {
  return <div>index</div>;
};

export default Assessment;
